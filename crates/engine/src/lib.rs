//! Deprecated shim — the engine moved to `lean_chain::engine` (commit 3.3).
//! This empty `lib.rs` preserves bisectability across the merge; commit 3.4
//! deletes the shim and removes `crates/engine` from the workspace members.
