//! Deprecated shim — `lean-beacon` binary moved to `bin/lean-beacon`
//! (commit 6.2). The reusable library surface lives at `beacon-cli`.
//! Commit 6.3 removes this stub and drops the workspace member.
