[package]
name          = "beacon"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
description   = "`lean-beacon` binary entry-point (CLI). Implementation lands in a later issue (Tier 7)."

[[bin]]
name = "lean-beacon"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
