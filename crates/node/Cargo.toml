[package]
name          = "node"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
description   = "Wires concrete services into a `Node`. Implementation lands in a later issue (Tier 7)."

[lints]
workspace = true

[dependencies]
